/// contains Map and impl
use crate::entities::tile::*;

/// main Map struct containing various Tile
pub struct Map {
    map: Vec<Vec<dyn Tile>>,
}

impl Map {
    fn init() -> Self {
        
    }
    
    fn 
}